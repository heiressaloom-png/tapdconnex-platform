const K={CAP:'tapd_captures',EV:'tapd_event_context',TPL:'tapd_selected_template',UID:'tapd_user_id'};
const T=[{"id":"opportunity","name":"Explore working together","tier":"starter","signals":["Active pain","Time pressure","Budget signals","Decision authority","Success criteria"],"questions":["What are you focused on at the moment?","What’s been taking up most of your time recently?","What’s working well and what’s proving challenging?","Is there anything you’re actively trying to improve?","Have you looked at other ways of solving that?","What would success look like for you?","What’s your timeline around this?","What’s the best next step from here?"],"signsNotReady":["Not a priority right now","No budget","Not their decision","Already sorted","Just being polite"]},{"id":"connector","name":"Grow my network / get introductions","tier":"starter","signals":["Referrals offered","Warm intros volunteered","Strong networks","Access to decision makers","Community involvement"],"questions":["What kind of people do you enjoy connecting?","Who are you currently working with?","What communities are you involved in?","Who should I know in your world?","Who do you think would benefit from what I do?","What’s the best way to approach them?","Would you be comfortable introducing us?","What would make that introduction easier?"],"signsNotReady":["Doesn't know them well","Needs to check first","Not the right person","Keeping you posted only"]},{"id":"stay_in_touch","name":"Keep the relationship warm","tier":"starter","signals":["Shared interests","Future goals","Personal chemistry","Long-term potential"],"questions":["What projects are exciting you right now?","Where do you see things heading?","What are you keeping an eye on?","What are you hoping to achieve this year?","Is there anything you’d like help with?","How can I support you?","What’s the best way to stay connected?","When should we reconnect?"],"signsNotReady":[]},{"id":"strategic_partnership","name":"Partner or collaborate","tier":"pro","signals":["Shared audience","Complementary strengths","Mutual benefit","Resource gaps","Distribution reach"],"questions":["What are you building at the moment?","Where do you see opportunities for collaboration?","What strengths does your organisation bring?","What’s missing today that would help you move faster?","What partnerships have worked well for you before?","What would success look like together?","Who else should be involved?","What should we explore next?"],"signsNotReady":["Needs leadership sign-off","One-sided benefit","Stretched on resources","Loose interest only"]},{"id":"speaking","name":"Get booked to speak","tier":"pro","signals":["Upcoming events","Audience fit","Topic relevance","Programme influence","Gaps in their lineup"],"questions":["What events are you involved with?","What topics resonate most with your audience?","What challenges are people talking about?","What type of speakers do you usually look for?","What outcomes do you want attendees to leave with?","What events are coming up?","How do you select speakers?","What should happen next?"],"signsNotReady":["Lineup already set","Someone else books speakers","Maybe a future event","Not right for their audience"]},{"id":"career","name":"Get hired or find work","tier":"pro","signals":["Hiring need","Skill gaps","Upcoming projects","Team fit signals","Budget for a role / contract"],"questions":["What initiatives are you focused on?","What skills are hardest to find right now?","What types of people do well in your environment?","What projects are coming up?","Where are you looking for support?","What experience would be most valuable?","What would success look like?","What’s the next step?"],"signsNotReady":["Not hiring now","Headcount freeze","HR handles it","Details kept on file only"]},{"id":"product_discovery","name":"Understand a problem","tier":"pro","signals":["Current workaround","Repeated frustration","Frequency of the problem","Cost / impact of it","Dissatisfaction with status quo"],"questions":["How are you currently solving that?","What’s frustrating about the current approach?","How often does this happen?","What impact does it have?","What have you already tried?","What would an ideal solution look like?","How would you know it worked?","Would you be open to testing something new?"],"signsNotReady":["Minor annoyance","Already have a fix","Rarely happens","Interesting but not for them"]},{"id":"pilot_beta","name":"Get something tested or piloted","tier":"pro","signals":["Willingness to test","Implementation readiness","Early-adopter behaviour","A specific use case","Expansion potential"],"questions":["Would you be open to testing something new?","What would make it worth trying?","Who would use it?","What would success look like?","What concerns would you have?","What support would you need?","How long would you want to test it?","What would happen if it worked?"],"signsNotReady":["Can't allocate anyone","Sees it as risky","Maybe next quarter","Needs procurement sign-off"]},{"id":"advisory","name":"Find an expert / get advice","tier":"pro","signals":["Deep expertise shown","Willingness to share","Strategic perspective","Pattern recognition","Openness to ongoing contact"],"questions":["What trends are you seeing?","What lessons have shaped your career?","If you were starting today, what would you do differently?","What’s a mistake people commonly make?","What opportunities do you think are overlooked?","What would you focus on if you were in my position?","How can I learn more about this area?","Would you be open to continuing the conversation?"],"signsNotReady":["Short on time","Not their area","Vague 'reach out sometime'","Only via paid program"]},{"id":"community_access","name":"Get into a community","tier":"pro","signals":["Active community involvement","Gatekeeper / organiser role","Open entry paths","Member value on offer","Good-fit signals for you"],"questions":["What communities are you involved in?","What makes that community valuable?","Who tends to thrive there?","How do people usually get involved?","What opportunities come from being part of it?","What do members contribute?","Do you think I’d be a good fit?","What would be the best way to get involved?"],"signsNotReady":["Invite-only / closed","No longer involved","Unsure on fit","Not taking new members"]}];let S={screen:'ready',tpl:localStorage.getItem(K.TPL)||'opportunity',ev:read(K.EV,null),p:{},blob:null,dur:0,t0:0,timer:null,rec:null,stream:null,chunks:[],paused:false,pausedMs:0,pauseStart:0};
function read(k,f){try{return JSON.parse(localStorage.getItem(k))||f}catch(e){return f}}function esc(s){return String(s||'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]))}function id(){return'cap_'+Date.now().toString(36)+'_'+Math.random().toString(36).slice(2,8)}function user(){let u=localStorage.getItem(K.UID);if(!u){u='user_'+Math.random().toString(36).slice(2,12);localStorage.setItem(K.UID,u)}return u}function msg(t){let e=document.getElementById('toast');e.textContent=t;e.classList.add('show');setTimeout(()=>e.classList.remove('show'),2200)}function tpl(){return T.find(x=>x.id===S.tpl)||T[0]}
function injectRecGuidanceCss(){if(document.getElementById('tapdRecGuidanceCss'))return;let s=document.createElement('style');s.id='tapdRecGuidanceCss';s.textContent=`
.rec-view{position:fixed;inset:0;z-index:80;display:flex;flex-direction:column;background:var(--bg,#F6F2EA);text-align:left}
.rec-header{flex:none;padding:16px 20px;background:rgba(246,242,234,.96);backdrop-filter:blur(12px);border-bottom:1px solid var(--soft,#EAE2D2);display:flex;justify-content:space-between;align-items:center;gap:14px}
.rec-template{font-family:var(--font-display,serif);font-size:18px;font-weight:800;line-height:1.2}
.rec-timer{font-family:var(--font-mono,monospace);font-size:18px;font-weight:900;color:var(--pro,#7C2D3F);font-variant-numeric:tabular-nums;display:flex;align-items:center;gap:8px;white-space:nowrap}
.rec-dot{width:10px;height:10px;border-radius:50%;background:var(--pro,#7C2D3F);animation:recPulse 1.6s infinite}
.rec-timer.paused .rec-dot{animation:none;background:var(--faint,#95887A)}
@keyframes recPulse{0%,100%{opacity:1}50%{opacity:.3}}
.rec-body{flex:1;overflow-y:auto;-webkit-overflow-scrolling:touch;padding:18px 20px 24px}
.rec-section{margin-bottom:22px}.rec-section-title{font-family:var(--font-mono,monospace);font-size:11px;letter-spacing:.12em;text-transform:uppercase;color:var(--faint,#95887A);font-weight:800;margin-bottom:10px}
.rec-chips{display:flex;flex-wrap:wrap;gap:8px}.rec-chip{background:var(--accent-bg,#E8EFEF);color:var(--accent,#1E4F5C);border-radius:999px;padding:7px 12px;font-size:13px;font-weight:800}
.rec-warn-chip{background:var(--pro-bg,#F5E8EC);color:var(--pro,#7C2D3F);border-radius:999px;padding:7px 12px;font-size:13px;font-weight:800}
.rec-questions{list-style:none;padding:0;margin:0}.rec-questions li{padding:13px 14px;margin-bottom:8px;border-radius:12px;background:var(--surface,#fff);border:1px solid var(--soft,#EAE2D2);font-size:15px;line-height:1.45;color:var(--text,#181410);display:flex;gap:10px;cursor:pointer}.rec-questions li .num{color:var(--accent,#1E4F5C);font-weight:900;flex:none}.rec-questions li.asked{opacity:.55;background:var(--surface-warm,#FBF8F2)}.rec-questions li.asked .num{text-decoration:line-through}
.rec-hint{text-align:center;font-size:13px;color:var(--muted,#5C5247);background:var(--surface-warm,#FBF8F2);border:1px solid var(--soft,#EAE2D2);border-radius:12px;padding:12px;margin-bottom:18px}
.rec-controls{flex:none;padding:16px 20px;padding-bottom:max(16px,env(safe-area-inset-bottom));background:rgba(246,242,234,.96);backdrop-filter:blur(12px);border-top:1px solid var(--soft,#EAE2D2);display:flex}.rec-one-btn{width:100%;border:0;border-radius:18px;padding:16px 18px;font:inherit;cursor:pointer;display:flex;align-items:center;justify-content:center;gap:12px;min-height:70px;background:var(--pro,#7C2D3F);color:#fff;box-shadow:0 12px 32px rgba(124,45,63,.22)}.rec-one-btn .main{display:block;font-size:17px;font-weight:900;line-height:1.1}.rec-one-btn .sub{display:block;font-size:11px;font-weight:800;opacity:.82;margin-top:4px;letter-spacing:.02em}.rec-one-btn.paused{background:var(--accent,#1E4F5C);box-shadow:0 12px 32px rgba(30,79,92,.20)}.rec-one-btn.stopping{transform:scale(.985);filter:brightness(.92)}
.pre-guide{margin-top:24px;padding:18px;border:1px solid var(--soft,#EAE2D2);border-radius:18px;background:var(--surface-warm,#FBF8F2);text-align:left}.pre-guide .rec-section{margin-bottom:18px}.pre-guide .rec-section:last-child{margin-bottom:0}.pre-guide-title{font-family:var(--font-display,serif);font-size:20px;font-weight:800;margin-bottom:6px}.pre-guide-copy{font-size:14px;color:var(--muted,#5C5247);line-height:1.45;margin-bottom:16px}
`;document.head.appendChild(s)}
injectRecGuidanceCss();
function readyGuide(t){let signals=(t.signals||[]).map(s=>`<span class="rec-chip">${esc(s)}</span>`).join(''),questions=(t.questions||[]).map((q,i)=>`<li><span class="num">${i+1}.</span><span>${esc(q)}</span></li>`).join(''),warn=(t.signsNotReady&&t.signsNotReady.length)?`<div class="rec-section"><div class="rec-section-title">Signs it may not be ready</div><div class="rec-chips">${t.signsNotReady.map(s=>`<span class="rec-warn-chip">${esc(s)}</span>`).join('')}</div></div>`:'';return `<div class="pre-guide"><div class="pre-guide-title">Before you capture</div><div class="pre-guide-copy">Quickly glance at what this template listens for. You do not need to ask every question — the conversation can stay natural.</div><div class="rec-section"><div class="rec-section-title">Signals to listen for</div><div class="rec-chips">${signals}</div></div>${warn}<div class="rec-section"><div class="rec-section-title">Questions you can use</div><ol class="rec-questions">${questions}</ol></div></div>`}


function dateNow(){let d=new Date();return d.toLocaleDateString([], {day:'2-digit',month:'short',year:'numeric'})+' • '+d.toLocaleTimeString([], {hour:'2-digit',minute:'2-digit'})}
function render(){document.getElementById('actionBar').style.display='none';document.getElementById('view').innerHTML=S.screen==='ready'?ready():S.screen==='recording'?recording():S.screen==='review'?review():saved();bind();window.scrollTo(0,0)}
function side(){let e=S.ev;if(!e)return `<p class="side-copy">Set this once. Everyone you capture here is grouped together.</p><button class="button-ghost" id="evBtn" style="width:100%">Add event context</button>`;return `<div class="detail"><div>📅</div><div><div class="k">Event</div><div class="v">${esc(e.name)}</div></div></div><div class="detail"><div>⇄</div><div><div class="k">Session</div><div class="v">${esc(e.session||'')}</div></div></div><button class="button-ghost" id="evBtn" style="width:100%">Edit event context</button>`}
function ready(){let t=tpl(),e=S.ev;return `<div class="wrap"><section class="main-card"><div class="capture-head"><div><div class="label">Using template</div><div class="template-name">${t.name}<span class="badge ${t.tier==='pro'?'pro':''}">${t.tier==='pro'?'Pro':'Starter'}</span></div></div><button class="button-ghost" id="tplBtn">⇄ Change template</button></div><div class="event-strip"><div>${e?`<strong>Event context:</strong> ${esc([e.name,e.session,e.location,e.date].filter(Boolean).join(' • '))}`:'<em>No event set — add one to group everyone you meet here.</em>'}</div><button class="button-ghost" id="evBtn2">${e?'Edit event':'Add event'}</button></div><div class="capture-stage"><div class="soundwave"></div><button class="capture-btn" id="capBtn"><span class="people">👥</span><span>Capture this moment</span></button><div class="capture-hint">Tap to start recording. You can stop any time.</div>${readyGuide(t)}<div class="trust-row"><div class="trust-item"><span class="trust-ico">◉</span><span><strong>High quality</strong><br>Audio ready</span></div><div class="trust-item"><span class="trust-ico">◎</span><span><strong>Listening for what matters</strong><br>${esc(t.name)} template</span></div><div class="trust-item"><span class="trust-ico">◇</span><span><strong>Private & secure</strong><br>Only you can see this</span></div></div></div></section><aside class="side-card"><div class="side-title"><div class="side-icon">🗓</div><h2>Event Context</h2></div>${side()}</aside></div>`}
function recording(){let t=tpl(),signals=(t.signals||[]).map(s=>`<span class="rec-chip">${esc(s)}</span>`).join(''),questions=(t.questions||[]).map((q,i)=>`<li data-q="${i}"><span class="num">${i+1}.</span><span>${esc(q)}</span></li>`).join(''),warn=(t.signsNotReady&&t.signsNotReady.length)?`<div class="rec-section"><div class="rec-section-title">Signs it may not be ready</div><div class="rec-chips">${t.signsNotReady.map(s=>`<span class="rec-warn-chip">${esc(s)}</span>`).join('')}</div></div>`:'';return `<div class="rec-view" id="recView"><div class="rec-header"><span class="rec-template">${esc(t.name)}</span><span class="rec-timer" id="recTimer"><span class="rec-dot"></span><span id="time">0:00</span></span></div><div class="rec-body"><div class="rec-hint">Conversations flow naturally — these are here if you want them. You do not need to ask them all.</div><div class="rec-section"><div class="rec-section-title">Signals to listen for</div><div class="rec-chips">${signals}</div></div>${warn}<div class="rec-section"><div class="rec-section-title">Questions</div><ol class="rec-questions" id="recQuestions">${questions}</ol></div></div><div class="rec-controls"><button class="rec-one-btn" id="recControlBtn" type="button"><span class="icon">⏸</span><span><span class="main">Pause recording</span><span class="sub">Tap to pause · hold to stop</span></span></button></div></div>`}
function review(){let t=tpl(),p=S.p||{},opts=t.signals.map(x=>`<option ${p.signal===x?'selected':''}>${x}</option>`).join('');return `<div class="wrap"><section class="main-card"><div class="review-view"><div class="label">Review & save</div><h1 class="template-name" style="margin:12px 0 20px">Lock in the relationship</h1><div class="notice">Recording captured. AI summary will populate this once OpenAI is connected. For now, save the key details while fresh.</div><div class="form"><div class="row"><div class="field"><label>Who did you meet?</label><input class="input" id="nm" placeholder="Name" value="${esc(p.name)}"></div><div class="field"><label>Company</label><input class="input" id="co" placeholder="Company" value="${esc(p.company)}"></div></div><div class="field"><label>Role / title</label><input class="input" id="ro" placeholder="e.g. Head of Partnerships" value="${esc(p.role)}"></div><div class="field"><label>What was discussed</label><textarea class="textarea" id="su" placeholder="What mattered, why it is worth following up…">${esc(p.summary)}</textarea></div><div class="row"><div class="field"><label>Signal</label><select class="input" id="sg"><option value="">— none —</option>${opts}</select></div><div class="field"><label>Momentum</label><select class="input" id="mo"><option value="">— none —</option>${['Act Soon','Keep Warm','Worth Exploring','Low Priority'].map(x=>`<option ${p.momentum===x?'selected':''}>${x}</option>`).join('')}</select></div></div><div class="field"><label>Next step</label><input class="input" id="nx" placeholder="e.g. Send the case study and book a call" value="${esc(p.nextStep)}"></div><div class="intelligence-card"><div class="label">Relationship intelligence</div><div class="intelligence-grid"><div class="intel-chip"><strong>Draft status</strong>Draft Ready</div><div class="intel-chip"><strong>AI status</strong>Manual pending OpenAI</div><div class="intel-chip"><strong>Completeness</strong>${complete(p)}%</div></div></div></div></div></section><aside class="side-card"><div class="side-title"><div class="side-icon">▤</div><h2>${esc(t.name)}</h2></div><p class="side-copy">This capture will be saved with the current template and event context.</p>${side()}</aside></div>`}
function saved(){return `<div class="saved-view"><div style="font-size:54px;color:var(--success)">✓</div><h1 class="template-name" style="margin:12px 0">Saved to your Hub.</h1><p class="side-copy">${esc(S.last||'This person')} is in your Relationship Hub.</p><div style="max-width:430px;margin:26px auto;display:grid;gap:12px"><a class="btn btn-primary" href="relationship-hub.html">Go to Relationship Hub</a><button class="btn btn-secondary" id="again">👥 Capture another person</button></div></div>`}
function bind(){let a=id=>document.getElementById(id);if(S.screen==='ready'){a('capBtn').onclick=start;a('tplBtn').onclick=pickTemplate;a('topTemplates').onclick=pickTemplate;(a('evBtn')||{}).onclick=eventBox;(a('evBtn2')||{}).onclick=eventBox}if(S.screen==='recording'){document.getElementById('actionBar').style.display='none';bindOneRecordingButton();let q=document.getElementById('recQuestions');if(q)q.onclick=e=>{let li=e.target.closest('li');if(li)li.classList.toggle('asked')}}if(S.screen==='review'){document.getElementById('actionBar').style.display='block';document.getElementById('actionInner').innerHTML='<button class="btn btn-secondary" id="discard">Discard</button><button class="btn btn-primary" id="save">Save to Relationship Hub</button>';a('save').onclick=save;a('discard').onclick=()=>{S.p={};S.screen='ready';render()}}if(S.screen==='saved')a('again').onclick=()=>{S.p={};S.screen='ready';render()}}
async function start(){S.chunks=[];if(!navigator.mediaDevices||!navigator.mediaDevices.getUserMedia||typeof MediaRecorder==='undefined'){S.screen='review';render();return}try{S.stream=await navigator.mediaDevices.getUserMedia({audio:true});S.rec=new MediaRecorder(S.stream);S.rec.ondataavailable=e=>{if(e.data&&e.data.size)S.chunks.push(e.data)};S.rec.onstop=finish;S.rec.start();S.t0=Date.now();S.paused=false;S.pausedMs=0;S.pauseStart=0;S.screen='recording';render();S.timer=setInterval(()=>{let el=document.getElementById('time');if(el){let now=S.paused?S.pauseStart:Date.now();let s=Math.max(0,Math.floor((now-S.t0-S.pausedMs)/1000));el.textContent=Math.floor(s/60)+':'+String(s%60).padStart(2,'0')}},250)}catch(e){S.screen='review';render()}}
function updateRecControlButton(){let b=document.getElementById('recControlBtn'),tm=document.getElementById('recTimer');if(!b)return;b.classList.toggle('paused',!!S.paused);if(tm)tm.classList.toggle('paused',!!S.paused);b.innerHTML=S.paused?'<span class="icon">▶</span><span><span class="main">Resume recording</span><span class="sub">Tap to resume · hold to stop</span></span>':'<span class="icon">⏸</span><span><span class="main">Pause recording</span><span class="sub">Tap to pause · hold to stop</span></span>'}
function bindOneRecordingButton(){let b=document.getElementById('recControlBtn');if(!b)return;let holdTimer=null,held=false;let clear=()=>{if(holdTimer){clearTimeout(holdTimer);holdTimer=null}};b.onpointerdown=()=>{held=false;b.classList.add('stopping');holdTimer=setTimeout(()=>{held=true;clear();stop()},850)};b.onpointerup=()=>{b.classList.remove('stopping');clear();if(!held)pauseRec()};b.onpointerleave=()=>{b.classList.remove('stopping');clear()};b.onpointercancel=()=>{b.classList.remove('stopping');clear()};updateRecControlButton()}
function pauseRec(){if(!S.rec)return;if(!S.paused){S.paused=true;S.pauseStart=Date.now();try{if(S.rec.state==='recording')S.rec.pause()}catch(e){}}else{S.paused=false;S.pausedMs+=Date.now()-S.pauseStart;try{if(S.rec.state==='paused')S.rec.resume()}catch(e){}}updateRecControlButton()}function stop(){clearInterval(S.timer);S.dur=(S.paused?S.pauseStart:Date.now())-S.t0-S.pausedMs;if(S.rec&&S.rec.state!=='inactive')S.rec.stop();else finish()}function finish(){try{if(S.chunks.length)S.blob=new Blob(S.chunks,{type:'audio/webm'})}catch(e){}if(S.stream)S.stream.getTracks().forEach(t=>t.stop());S.screen='review';render()}
function pullForm(){let v=id=>(document.getElementById(id)||{}).value||'';S.p={name:v('nm').trim(),company:v('co').trim(),role:v('ro').trim(),summary:v('su').trim(),signal:v('sg').trim(),momentum:v('mo').trim(),nextStep:v('nx').trim()}}
function complete(p){return Math.round([p.name,p.company,p.role,p.summary,p.nextStep].filter(Boolean).length/5*100)}function pri(m,s){let v=(m||s||'').toLowerCase();if(/act|urgent|opportunity|buy|pilot|pricing|proposal/.test(v))return'act-soon';if(/low|warm|stay/.test(v))return'keep-warm';return'worth-exploring'}function sig(t){if(t.id==='connector'||t.id==='community_access')return'connector';if(['strategic_partnership','advisory'].includes(t.id))return'collaboration';if(t.id==='stay_in_touch')return'stay';return'opportunity'}function ctx(ev,p){return `${ev&&ev.name?'Met at '+[ev.name,ev.location,ev.session,ev.date].filter(Boolean).join(', ')+'.':'Captured conversation.'} ${p.summary||''}`.trim()}function draft(p,c){let f=(p.name||'there').split(' ')[0];return `Hi ${f},\n\nGreat meeting you${c.event&&c.event!=='No event set'?' at '+c.event:''}. I captured this context so I could follow up properly: ${p.summary||'we had a useful conversation worth continuing.'}${p.nextStep?'\n\nSuggested next step: '+p.nextStep:''}\n\nBest,\n[Your name]`}
function save(){try{pullForm();let t=tpl(),e=S.ev,p=S.p,evText=e&&e.name?[e.name,e.session,e.location].filter(Boolean).join(' · '):'No event set';let c={id:id(),name:p.name||null,company:p.company||null,role:p.role||null,signal:sig(t),signalLabel:p.signal||p.momentum||t.name+' capture',evidence:p.summary||p.signal||p.nextStep||'Manual capture saved for review',priority:pri(p.momentum,p.signal),action:p.nextStep||'Review and decide the next step',context:ctx(e,p),capturedAt:new Date().toISOString(),capturedDaysAgo:0,event:evText,eventKey:e&&e.key?e.key:null,outcome:'active',template:t.id,templateName:t.name,needsNameConfirmation:!p.name,nameConfidence:p.name?'confirmed':null,completeness:complete(p),hasAudio:!!S.blob,audioDurationMs:S.dur||0,aiStatus:S.blob?'manual_pending_openai':'manual_entry',draftStatus:'draft_ready',intelligenceFrameworkVersion:(window.TAPD_INTELLIGENCE&&TAPD_INTELLIGENCE.version)||'1.4.0'};c.draftMessage=draft(p,c);let list=read(K.CAP,[]);list.unshift(c);localStorage.setItem(K.CAP,JSON.stringify(list));if(window.TapdSync)TapdSync.pushCapture(c);S.last=c.name||'Unnamed contact';S.screen='saved';render();msg('Saved to Relationship Hub')}catch(e){console.error(e);msg('Could not save. Try again.')}}
function pickTemplate(){let sh=document.getElementById('tplSheet');sh.innerHTML='<div class="grip"></div><h2>Choose a template</h2><div class="sheet-sub">Select the template for this capture.</div>'+T.map(t=>`<button class="tpl-option ${t.id===S.tpl?'selected':''}" data-id="${t.id}"><div><div class="tpl-name">${esc(t.name)}</div><div class="tpl-sum">${esc(t.tier)}</div></div><span class="badge ${t.tier==='pro'?'pro':''}">${t.tier==='pro'?'Pro':'Starter'}</span></button>`).join('');sh.querySelectorAll('.tpl-option').forEach(b=>b.onclick=()=>{S.tpl=b.dataset.id;localStorage.setItem(K.TPL,S.tpl);close('tplOverlay');render();msg('Template selected')});open('tplOverlay')}
function eventBox(){let e=S.ev||{};let sh=document.getElementById('eventSheet');sh.innerHTML=`<div class="grip"></div><h2>Event context</h2><div class="sheet-sub">Set this once. Everyone you capture here is grouped together.</div><div class="form"><div class="field"><label>Event name</label><input class="input" id="en" value="${esc(e.name||'')}" placeholder="e.g. AI Expo"></div><div class="row"><div class="field"><label>Location / area</label><input class="input" id="el" value="${esc(e.location||'')}"></div><div class="field"><label>Session</label><input class="input" id="es" value="${esc(e.session||'')}"></div></div><div class="field"><label>Date / time</label><input class="input" id="ed" value="${esc(e.date||dateNow())}"></div><button class="btn btn-primary" id="evSave">Save event context</button></div>`;sh.querySelector('#evSave').onclick=()=>{let n=document.getElementById('en').value.trim();if(!n){msg('Give the event a name');return}S.ev={name:n,location:document.getElementById('el').value.trim(),session:document.getElementById('es').value.trim(),date:document.getElementById('ed').value.trim(),key:slug(n)+'-'+Date.now()};localStorage.setItem(K.EV,JSON.stringify(S.ev));close('eventOverlay');render();msg('Event context saved')};open('eventOverlay')}
function open(x){document.getElementById(x).classList.add('show')}function close(x){document.getElementById(x).classList.remove('show')}['tplOverlay','eventOverlay'].forEach(x=>document.getElementById(x).onclick=e=>{if(e.target.id===x)close(x)});render();

/* ============================================================

   ROUND 8 — Gold-Star Calibration feedback capture.

   Stores a learning SIGNAL. Does NOT touch the intelligence score.
   Exposed as window.tapdSaveFeedback for the Relationship Hub UI to call.

   ============================================================ */

(function () {
  // User-facing feedback option → internal learning signal.
  // This is documented and stored only. It does not rewrite any intelligence score.
  const FEEDBACK_LEARNING_MAP = {
    wrong_priority: 'score_may_be_too_aggressive',
    missing_context: 'completeness_probe_needs_improvement',
    not_opportunity: 'opportunity_classification_too_aggressive',
    already_handled: 'closure_happened_off_platform',
    wrong_next_step: 'layer9_next_step_needs_correction',
    other: 'open_ended_for_review',
  };

  window.tapdSaveFeedback = async function tapdSaveFeedback({ engagementId, option, note }) {
    const record = {
      engagement_id: engagementId,
      type: 'interpretation',
      feedback_option: option,
      learning_signal: FEEDBACK_LEARNING_MAP[option] || FEEDBACK_LEARNING_MAP.other,
      note: note || null,
      created_at: new Date().toISOString(),
    };

    // Preferred path: save to Supabase if the client is available.
    try {
      const client = window.supabase || window.supabaseClient || null;

      if (client && client.auth && typeof client.auth.getUser === 'function') {
        const {
          data: { user },
        } = await client.auth.getUser();

        const { error } = await client
          .from('feedback')
          .insert({ ...record, user_id: user?.id || null });

        if (error) throw error;

        return { saved: true, via: 'supabase' };
      }
    } catch (e) {
      console.error('supabase feedback insert failed, falling back to local', e);
    }

    // Fallback path: queue locally so the product works before DB/auth is ready.
    try {
      const key = 'tapd_feedback_queue';
      const queue = JSON.parse(localStorage.getItem(key) || '[]');
      queue.push(record);
      localStorage.setItem(key, JSON.stringify(queue));

      return { saved: true, via: 'local' };
    } catch (e) {
      console.error('local feedback save failed (non-blocking)', e);
      return { saved: false };
    }
  };

  // Optional: expose the map for future debugging / migration scripts.
  window.tapdFeedbackLearningMap = FEEDBACK_LEARNING_MAP;
})();
